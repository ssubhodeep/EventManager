{
  "name": "functions",
  "description": "Scheduled Cloud Function that sends automatic FCM event-reminder pushes for Event Manager",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0",
    "firebase-functions": "^5.0.0"
  }
}
